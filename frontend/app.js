const createCharacterButton = document.getElementById(
    "createCharacterButton"
);

createCharacterButton.addEventListener("click", () => {
    alert(
        "The character creation page will be added in the next development step."
    );
});
